<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @admin_subject.admin_subject_forms.map{|subject| subject.name } %>
</p>

<p>
  <strong>Course:</strong>
  <%= @admin_subject.course.name %>
</p>

<p>
  <strong>Semester:</strong>
  <%= @admin_subject.semester.name %>
</p>

<%= link_to 'Edit', edit_admin_subject_path(@admin_subject) %> |
<%= link_to 'Back', admin_subjects_path %>
<%= render 'shared/extends_admin_layout' %>
