<%= form_for(admin_tuition_fees_group) do |f| %>
  <% if admin_tuition_fees_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_tuition_fees_group.errors.count, "error") %> prohibited this admin_tuition_fees_group from being saved:</h2>

      <ul>
      <% admin_tuition_fees_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
