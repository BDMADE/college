<%= form_for(admin_postal_type) do |f| %>
  <% if admin_postal_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_postal_type.errors.count, "error") %> prohibited this admin_postal_type from being saved:</h2>

      <ul>
      <% admin_postal_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :icon_id %>
    <%#= f.text_field :icon_id %>
    <%= f.collection_select :icon_id,Admin::Icon.all.map,:id,:name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
