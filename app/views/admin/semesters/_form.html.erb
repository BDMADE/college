<%= form_for(admin_semester) do |f| %>
  <% if admin_semester.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_semester.errors.count, "error") %> prohibited this admin_semester from being saved:</h2>

      <ul>
      <% admin_semester.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :short_form %>
    <%= f.text_field :short_form %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
