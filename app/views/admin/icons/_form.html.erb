<%= form_for(admin_icon) do |f| %>
  <% if admin_icon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_icon.errors.count, "error") %> prohibited this admin_icon from being saved:</h2>

      <ul>
      <% admin_icon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :icon %>
    <%= f.text_field :icon %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
